public class Main {
  static int enemyList[] = {};
  
  public static void main(String[] args) {
    /*
     * OK so
     * the plan
     * we have a loop that gets a character in
     * then we handle said keypress
     * if touching a coin -> collect it, add it to score, and spawn both an enemy and a coin
     * if we are near coin -> give coin a value
     * if we are touching an enemy -> kill player, end game
     * then move the player
     * then update all the enemies in the enemy list by iterating over it
     * finally update and draw the map
     * 
     * CHANGE OF PLANS
     * EVERYTHING IS ON A WINDOW NOW
     * WHYYYYY
     * 
     */
    
  }
}
